# auramate

灵息 · AuraMate — A gentle tap, not an interruption.

Local-first desktop companion for work–rest rhythm. Product intent: `docs/requirement.md`. Phase 1 buildable plan: `docs/spec.md`. Domain vocabulary: `CONTEXT.md`.

## Develop

```bash
pnpm install
pnpm tauri dev
```

Stack: Tauri 2 + React + TypeScript (see `docs/adr/0001-react-ui.md`). Agent commands: `AGENTS.md`.
